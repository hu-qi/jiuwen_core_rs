# 工具插件

工具插件把文件、Shell、代码、Web 或远程协议能力暴露给 Agent Runtime。

## 工具契约

每个工具应定义：

- 稳定名称和描述
- 输入和输出 Schema
- 权限范围
- 超时和取消行为
- 错误分类
- 资源清理策略

## 安全要求

工具参数必须在运行时校验。文件路径、命令、网络地址和凭据都需要独立权限控制；不能只依赖 Prompt 约束危险操作。

工具注册应返回 Effect，工具创建的子进程、Socket、临时文件和后台任务必须在卸载时释放。MCP 工具还需要验证 Transport 生命周期和能力协商。
