[package]
name = "ah-plugins-queue"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real file-backed message queue: append-only JSONL per channel with persisted consume cursor (log + offset model)"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
