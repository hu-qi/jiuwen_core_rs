[package]
name = "ah-plugins-dataset-curator"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real replay-dataset curation from eval artifacts: case decisions, provenance, targeted seed tasks, YAML report"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
serde_yaml = "0.9"
