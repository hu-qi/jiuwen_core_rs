[package]
name = "ah-plugins-code"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real code execution: write code to isolated scratch dir, run python3 subprocess with timeout, capture output, cleanup"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "time"] }
