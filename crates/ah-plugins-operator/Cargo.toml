[package]
name = "ah-plugins-operator"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real self-evolution operators: LLM/tool/memory/skill parameter handles with tunables, freeze, state, and update callbacks"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
