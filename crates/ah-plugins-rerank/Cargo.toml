[package]
name = "ah-plugins-rerank"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real retrieval reranker: weighted fusion of lexical + vector scores with diversity penalty (deterministic)"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
ureq = { version = "2.12", default-features = false }
serde_json = "1"
