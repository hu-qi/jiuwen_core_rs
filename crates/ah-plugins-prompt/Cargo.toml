[package]
name = "ah-plugins-prompt"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real file-backed versioned prompt registry with {{var}} rendering and explicit missing-variable errors"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
