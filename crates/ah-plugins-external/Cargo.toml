[package]
name = "ah-plugins-external"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real external CLI agent runtime: spawn third-party CLIs as team members (streaming stdin + re-invoke-per-turn flavors)"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros", "process", "io-util", "time", "sync"] }
