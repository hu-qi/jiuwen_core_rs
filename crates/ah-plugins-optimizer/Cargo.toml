[package]
name = "ah-plugins-optimizer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real textual-gradient optimizer: derives updates from evolution evaluations and applies them through the operator seam (freeze-aware)"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"

[dev-dependencies]
ah-plugins-operator = { workspace = true }
