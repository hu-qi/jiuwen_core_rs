[package]
name = "ah-plugins-team-scheduler"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real team scheduler scan core: per-member earliest PENDING(assignee) task pick + review round decisions"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
