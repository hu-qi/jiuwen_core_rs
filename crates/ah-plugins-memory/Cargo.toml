[package]
name = "ah-plugins-memory"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Persistent JSON-file or external KV-backed memory with remember/recall/forget tools"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros"] }

[dev-dependencies]
ah-plugins-tools = { workspace = true }
ah-plugins-store = { workspace = true }
