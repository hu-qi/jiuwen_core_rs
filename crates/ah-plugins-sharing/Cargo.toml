[package]
name = "ah-plugins-sharing"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real experience sharing: local filesystem hub backend + skill-scoped ExperienceSharer (stage/dedup/flush with retry & mirror cache)"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros", "time"] }
