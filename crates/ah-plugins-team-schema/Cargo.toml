[package]
name = "ah-plugins-team-schema"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real agent-team schema: ssh transport validation + task graph models + infra (transport/storage) registry + blueprint validations"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
