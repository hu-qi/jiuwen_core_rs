[package]
name = "ah-plugins-stream"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real session stream pipeline: async queue, emitter, writer manager, typed writers (OutputSchema/TraceSchema/CustomSchema)"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros", "sync", "time"] }
