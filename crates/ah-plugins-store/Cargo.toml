[package]
name = "ah-plugins-store"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real file-backed store: BaseKVStore (JSON files) and BaseMessageStore (JSONL channels); external Redis/GaussDB/ES backends later"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros"] }
