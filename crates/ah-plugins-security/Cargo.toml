[package]
name = "ah-plugins-security"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real security seam: rule-based guardrails (prompt injection, secrets) + tools/pre-execute rail"

[dependencies]
ah-contracts = { workspace = true }
ah-hub = { workspace = true }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros"] }
ureq = { version = "2.12", default-features = false }

[dev-dependencies]
ah-plugins-tools = { workspace = true }
